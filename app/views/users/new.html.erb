<% provide(:title, '新規登録') %>
<div class="signup-img">
  <div id="wrapper" class="container-signup">
    <%= render 'layouts/header' %>
    <div class="row justify-content-center">
      <div class="col-md-6">
        <p class="eng-title text-center">Sign up</p>
        <h1 class="jpn-title text-center">新規登録</h1>
        <div class="field text-center">
          <%= form_for(@user, url: signup_path) do |f| %>
            <%= render 'shared/error_messages', object: f.object %>
            <%= f.label :name, '名前（ニックネーム可）', class: 'd-block text-left mx-auto signup-label' %>
            <%= f.text_field :name, class: 'signup-form', placeholder: '（例）田中実' %>
            <% if f.object.errors.include?(:name) %>
              <p class="alert alert-danger error mx-auto text-left"><%= f.object.errors.full_messages_for(:name).first %></p>
            <% end %>
            <%= f.label :email, 'メールアドレス', class: 'd-block text-left mx-auto signup-label' %>
            <%= f.email_field :email, class: 'signup-form', placeholder: '（例）example@gmail.com' %>
            <% if f.object.errors.include?(:email) %>
              <p class="alert alert-danger error mx-auto text-left"><%= f.object.errors.full_messages_for(:email).first %></p>
            <% end %>
            <%= f.label :password, 'パスワード（６文字以上）', class: 'd-block text-left mx-auto signup-label' %>
            <%= f.password_field :password, class: 'signup-form', placeholder: '（例）foobar' %>
            <% if f.object.errors.include?(:password) %>
              <p class="alert alert-danger error mx-auto text-left"><%= f.object.errors.full_messages_for(:password).first %></p>
            <% end %>
            <%= f.label :password_confirmation, 'パスワード（確認）', class: 'd-block text-left mx-auto signup-label' %>
            <%= f.password_field :password_confirmation, class: 'signup-form', placeholder: '（例）foobar' %>
            <% if f.object.errors.include?(:password_confirmation) %>
              <p class="alert alert-danger error mx-auto text-left"><%= f.object.errors.full_messages_for(:password_confirmation).first %></p>
            <% end %>
            <%= f.submit '登録する', class: 'btn-style btn-signup mx-auto'%>
          <% end %>
        </div>
      </div>
    </div>
    <%= render 'layouts/footer' %>
  </div>
</div>
