<span class="like">
  <% if micropost.favorite?(current_user) %>
    <%= form_for(micropost.likes.find_by(user_id: current_user.id), method: :delete, remote: true) do |f| %>
      <%= button_tag(class: "btn btn-like icon-size") do %>
        <%= content_tag :span, "#{micropost.likes_count}", class: "fas fa-heart" %>
      <% end %>
    <% end %>
  <% else %>
    <%= form_for(micropost.likes.build, remote: true) do |f| %>
      <%= button_tag(class: "btn btn-like-non icon-size") do %>
        <%= hidden_field_tag :micropost_id, micropost.id %>
        <%= content_tag :span, "#{micropost.likes_count}", class: "far fa-heart" %>
      <% end %>
    <% end %>
  <% end %>
</span>
